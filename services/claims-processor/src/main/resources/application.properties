spring.application.name=claims-processor

# Database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/insureco}
spring.datasource.username=${DATABASE_USER:insureco}
spring.datasource.password=${DATABASE_PASSWORD:insureco123}
spring.datasource.driver-class-name=org.postgresql.Driver

# Kafka (Redis stub)
kafka.broker=${KAFKA_BROKER:localhost:6379}

# Logging
logging.level.root=INFO
logging.level.com.insureco=DEBUG